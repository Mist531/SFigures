<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sfigures">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SFigures">
        <activity android:name=".info"></activity>
        <activity android:name=".vivod_unluky" />
        <activity android:name=".vivod_lucky" />
        <activity android:name=".priamougol" />
        <activity android:name=".kvadrat" />
        <activity android:name=".oval" />
        <activity android:name=".Main"></activity>
        <activity android:name=".Loading">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>